version: 0.0
os: linux
files:
  - source:  /
    destination: /var/www/html
    overwrite: yes
 
permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

# hooks:
#   AfterInstall:
#     - location: scripts/start_server.sh
#       timeout: 60
#       runas: root
